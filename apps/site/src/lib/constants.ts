/**
 * Shared constants — the repo URL is referenced from the hero, self-host,
 * and footer sections; keep one source so a rename touches one file.
 */
export const REPO_URL = "https://github.com/ColoMiranda/where-we-were";
